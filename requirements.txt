# Fallback for Python hosts that do not select the repository's uv.lock.
jsonschema>=4.23,<5

# skills/optimize-search-visibility/scripts/* only — not a dependency of the
# signal-to-growth package itself, so these are not in pyproject.toml.
requests>=2.32.4,<3.0.0
beautifulsoup4>=4.15.0,<5.0.0
lxml>=6.0.2,<7.0.0
